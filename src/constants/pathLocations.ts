import {RootStackParamList} from '../typescript/types';
// Stacks paths
const bottomTabsStackPath = '/bottom-tabs-stack';

// Screen paths
const homePath: keyof RootStackParamList = '/home';
const listPath: keyof RootStackParamList = '/list';
const listStackPath: keyof RootStackParamList = '/list-stack';
const homeStackPath: keyof RootStackParamList = '/home-stack';
const infoCharacterPath: keyof RootStackParamList = '/info-stack'

export {
  bottomTabsStackPath,
  homePath,
  listPath,
  homeStackPath,
  listStackPath,
  infoCharacterPath
};
